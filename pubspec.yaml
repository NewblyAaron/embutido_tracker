name: embutido_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  cached_network_image: ^3.4.1
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.4.1
  flutter_map: ^8.1.1
  geolocator: ^14.0.2
  image: ^4.5.4
  image_picker: ^1.1.2
  latlong2: ^0.9.1
  logger: ^2.6.1
  provider: ^6.1.5
  supabase_flutter: ^2.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.5.0
  build_runner: ^2.6.0
  lefthook_dart:
    git:
      url: https://github.com/NewblyAaron/lefthook_dart
      ref: feat/windows-support

flutter:
  assets:
    - assets/
  uses-material-design: true